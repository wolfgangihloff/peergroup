= form_for([@supervision, SupervisionFeedback.new]) do |f|
  = f.text_area :content
  %p.inline-hints= t("supervision_feedbacks.form.content_hint")
  = submit_tag t("supervision_feedbacks.form.submit")
