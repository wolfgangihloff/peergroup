= form_for(IdeasFeedback.new, :url => supervision_ideas_feedback_path(supervision)) do |f|
  = f.text_area :content
  %p.inline-hints= t("ideas_feedbacks.form.content_hint")
  = submit_tag t("ideas_feedbacks.form.submit")
