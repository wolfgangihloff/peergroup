.topic_votes_part
  %h2= t("supervisions.show.topic_votes", :default => "Topic votes")

  - if @supervision.voting_on_topics?
    %p.waiting= t("supervisions.show.topic_votes_waiting", :default => "Waiting for all votes")

    - @supervision.topics.each do |topic|
      %div[topic]
        = render topic.user
        .content
          %p= topic_content(topic)
          = form_for([@supervision, topic, Vote.new]) do |f|
            = f.submit t("supervisions.show.submit_vote", :default => "Vote"), :disabled => @supervision.voted_on_topic?(current_user)
