%p
  = t("supervisions.show.supervision_finished")
  - if supervision.group.current_supervision.present?
    = link_to(t("supervisions.show.go_to_current"), supervision_step_path(supervision.group.current_supervision))
  - else
    = link_to(t("supervisions.show.create_new"), new_group_supervision_path(supervision.group))
