%h2 Edit user
- form_for(@user) do |f|
  = f.error_messages
  = render :partial => 'fields', :locals => { :f => f }
  .actions
    = f.submit "Update"
%div
  = gravatar_for @user
  %a{:href => "http://gravatar.com/emails", :target => "_blank"} change
