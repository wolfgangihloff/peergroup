= link_to 'Edit', edit_group_path(@group)
|
\#{link_to 'Back', groups_path}

- users_list(@group.members, @group.founder)

%dl.rules
  - @group.rules.each do |rule|
    %dt= rule.name
    %dl= markdown(rule.description)
