= link_to 'Edit', edit_group_path(@group)
|
\#{link_to 'Back', groups_path}

- users_list(@group.members, @group.founder)

.rules
  = render :partial => 'rules/rules'

  %ul.actions
    %li= link_to "New Rule", new_group_rule_path(@group)

